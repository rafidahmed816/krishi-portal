# AgroLink

AI-Driven Agricultural Marketplace & Intelligence Portal.

## Tech Stack
- **Frontend**: Next.js 14 (TypeScript, Tailwind CSS)
- **Backend**: Python FastAPI (Clean Architecture)
- **Auth**: AWS Cognito
- **Database**: PostgreSQL (AWS RDS)
- **Cloud**: AWS EC2, S3, Lambda, CloudWatch

## Quick Start
```bash
# Frontend
cd frontend && npm run dev

# Backend
cd backend && source venv/bin/activate && uvicorn app.main:app --reload --port 8000
```

## Agent Handoff
If you're an AI agent continuing this project, read `.agent/PROJECT_CONTEXT.md` first, then `.agent/CURRENT_TASK.md`.
